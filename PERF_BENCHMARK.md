# H5 性能优化记录

> 测量方式：同一台开发机上分别执行 `npm run build:h5`，记录 Taro CLI 输出的产物体积。时间：2026-03-09。

## 本轮优化

- 将 `src/services/api.ts` 中的运行时响应校验从 `zod` 改为“生产最小校验 + 开发详细校验”。
- 保留开发期的结构校验信息，生产包不再为详细 schema 付出体积成本。

## 结果对比

| 指标 | 优化前 | 优化后 | 变化 |
|------|--------|--------|------|
| `chunk/*.js` 总体积 | `1.07 MiB` | `385 KiB` | 下降约 `64%` |
| 最大页面 JS chunk | `203 KiB` | `85.3 KiB` | 下降约 `58%` |
| 第二大页面 JS chunk | `181 KiB` | `62.8 KiB` | 下降约 `65%` |
| 构建告警 | 存在超限告警 | 无超限告警 | 已消除 |

## 面试可讲版本

- 问题：多端用户端为了兼顾接口容错，在运行时引入了较重的 schema 校验，导致 H5 包体偏大。
- 取舍：生产环境保留最小校验保证主链路可用，开发环境保留详细结构校验方便排障，不再让调试能力直接进入生产包。
- 实现：移除 `zod` 的运行时依赖，改为轻量校验函数，并按环境切换校验强度。
- 验证：同一命令构建对比，`chunk/*.js` 从 `1.07 MiB` 降到 `385 KiB`，同时现有 `68` 条单测保持通过。
